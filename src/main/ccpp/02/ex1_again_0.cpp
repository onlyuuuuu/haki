#include <bits/stdc++.h>

using namespace std;

int main()
{
    int i, j, k;
    int numberOfTests;
    int numberOfJudges;
    int numberOfOppPerFight;
    int left, right;
    string result = "";

    cin >> numberOfTests;
    for (i = 0; i < numberOfTests; ++i)
    {
        // init
        cin >> numberOfJudges;
        cin >> numberOfOppPerFight;
        int apm[numberOfJudges];

        // TODO: This should be done exactly like ex2 but too lazy for it

        result.append(" ");

        result.append("\n");
    }
    cout << result << endl;
}
