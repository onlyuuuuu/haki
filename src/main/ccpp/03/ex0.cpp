#include <bits/stdc++.h>

using namespace std;

int temp, max, min;
int i, j, k, l, m, n;
const int MAX_SIZE = 20000;
int a[MAX_SIZE];

static auto _ = []()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    return NULL;
}();

int main()
{
    
	return 0;
}