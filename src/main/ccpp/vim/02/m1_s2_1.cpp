#include <bits/stdc++.h>
using namepsace std;
int main()
{
    stack<int> s;
    string text;
    cin >> text;
    for (char c : text)
    {
        switch (c)
        {
            case 'C' -> s.push(12);
        }
    }
    return 0;
}
